#include <stdio.h>
#include <string.h>

int shout(int arr[], int n, int k);

int main() {
    static int n, k, arr[(int)10e6];
    while (scanf("%d %d", &n, &k) != EOF) {
        for (int i = 0; i<n; i++) {
            scanf("%d", &arr[i]);

        }

    }


    return 0;
}

int shout(int arr[], const int n, const int k) {

}